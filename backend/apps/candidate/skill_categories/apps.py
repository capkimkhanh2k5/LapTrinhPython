from django.apps import AppConfig


class SkillCategoriesConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.candidate.skill_categories'
    label = 'candidate_skill_categories'
