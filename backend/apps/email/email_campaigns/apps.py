from django.apps import AppConfig


class EmailCampaignsConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.email.email_campaigns'
    label = 'email_email_campaigns'
