from django.apps import AppConfig

class ReferralsConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.recruitment.referrals'
    label = 'recruitment_referrals'
    verbose_name = 'Referrals'
