from django.apps import AppConfig


class SavedJobsConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.recruitment.saved_jobs'
    label = 'recruitment_saved_jobs'
