from django.apps import AppConfig


class ReferralProgramsConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.social.referral_programs'
    label = 'social_referral_programs'
